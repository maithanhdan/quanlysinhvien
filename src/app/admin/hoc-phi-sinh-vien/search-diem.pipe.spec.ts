import { SearchDiemPipe } from './search-diem.pipe';

describe('SearchDiemPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDiemPipe();
    expect(pipe).toBeTruthy();
  });
});
